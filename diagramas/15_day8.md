```mermaid
graph TD
    A[Inicio] --> B{Leer coordenadas de nodos 3D desde 'day8.txt'};
    B --> C{Calcular todas las aristas posibles (distancia entre cada par de nodos)};
    C --> D{Guardar aristas en un Min-Heap (ordenadas por distancia ascendente)};
    D --> E{Inicializar estructura Union-Find (DSU) con un componente para cada nodo};
    E --> F{Procesar 999 aristas del Min-Heap (las más cortas)};
    F -- Para cada arista (u, v) --> G{Encontrar los componentes raíz de 'u' y 'v'};
    G -- ¿Pertenecen a componentes diferentes? --> H{Sí: Unir los dos componentes};
    G -- No --> F;
    H --> F;
    F -- Fin del bucle --> I{Obtener la lista de tamaños de todos los componentes del DSU};
    I --> J{Ordenar los tamaños de mayor a menor};
    J --> K{Tomar los 3 tamaños más grandes y multiplicarlos};
    K --> L{Imprimir el resultado};
    L --> M[Fin];

    style A fill:#2E8B57,color:#fff
    style M fill:#2E8B57,color:#fff
```
