```mermaid
graph TD
    A[Inicio] --> B{Leer 'day3.txt' y sumar resultados por línea};
    B -- Para cada línea --> C{Convertir línea a vector de dígitos 'line'};
    C --> D{Crear tabla DP de 13 x (longitud(line)+1)};
    D --> E{Establecer caso base: dp[0][0] = 0};
    E --> F{Bucle anidado:<br/>- Por cada dígito 'i' en 'line'<br/>- Por cada número de elementos 'e' de 0 a 11};
    F -- Si dp[e][i] es alcanzable --> G{Procesar Transiciones};
    G -- Transición 1 (No usar dígito 'i') --> H{dp[e][i+1] = max(dp[e][i+1], dp[e][i])};
    G -- Transición 2 (Añadir dígito 'i') --> I{nuevo_numero = dp[e][i] * 10 + line[i]<br/>dp[e+1][i+1] = max(dp[e+1][i+1], nuevo_numero)};
    H --> F;
    I --> F;
    F -- Fin de los bucles --> J{Encontrar el valor máximo en la última fila de la tabla DP (dp[12])};
    J --> K{Sumar este valor máximo al total acumulado};
    K --> B;
    B -- Fin de las líneas --> L{Imprimir resultado final};
    L --> M[Fin];

    style A fill:#2E8B57,color:#fff
    style M fill:#2E8B57,color:#fff
```
