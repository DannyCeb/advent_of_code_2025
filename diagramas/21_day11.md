```mermaid
graph TD
    A[Inicio] --> B{Parsear 'day11.txt' para construir un grafo dirigido};
    B -- Para cada línea 'nodo: n1 n2 ...' --> C{Añadir aristas (nodo, n1), (nodo, n2), etc. al grafo};
    C --> D{Inicializar:<br/>- 'result' = 0<br/>- 'queue' con el nodo de inicio "you"};
    D --> E{Mientras la 'queue' no esté vacía};
    E -- Sacar 'node' de la 'queue' --> F{¿Es 'node' el objetivo "out"?};
    F -- Sí --> G{Incrementar 'result';<br/>Continuar al siguiente ciclo};
    G --> E;
    F -- No --> H{Obtener los vecinos ('neis') de 'node' desde el grafo};
    H -- Para cada 'nei' --> I{Añadir 'nei' al final de la 'queue'};
    I --> H;
    H -- Fin de los vecinos --> E;
    E -- Queue vacía --> J{Imprimir 'result'};
    J --> K[Fin];

    style K fill:#2E8B57,color:#fff
```
