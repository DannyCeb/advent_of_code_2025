```mermaid
graph TD
    A[Inicio] --> B{Leer 'day7.txt' en una matriz 2D};
    B --> C{Encontrar columna 'start_col' del carácter 'S' en la primera fila};
    C --> D{Crear tabla DP (mismo tamaño que la matriz) e inicializarla a 0};
    D --> E{Establecer caso base: dp[0][start_col] = 1};
    E --> F{Iterar por filas 'i' y columnas 'j' de la matriz};
    F -- Si dp[i][j] > 0 --> G{Evaluar el carácter matrix[i][j]};
    G -- '.' o 'S' o '|' --> H{Mover hacia abajo:<br/>dp[i+1][j] += dp[i][j]};
    G -- '^' --> I{Mover en diagonal:<br/>dp[i+1][j-1] += dp[i][j] (si es posible)<br/>dp[i+1][j+1] += dp[i][j] (si es posible)};
    G -- Otro carácter --> J{No hacer nada};
    H --> F;
    I --> F;
    J --> F;
    F -- Fin de los bucles --> K{Calcular 'result' como la suma de la última fila de la tabla DP};
    K --> L{Imprimir 'result'};
    L --> M[Fin];

    style A fill:#2E8B57,color:#fff
    style M fill:#2E8B57,color:#fff
```
